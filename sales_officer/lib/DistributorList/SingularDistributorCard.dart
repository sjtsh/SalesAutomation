import 'package:flutter/material.dart';

class SingularDistributorCard extends StatelessWidget {
  const SingularDistributorCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
